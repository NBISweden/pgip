name: pgip-dev
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - conda-lock
  - pip
  - pre-commit
  - r-docopt
  - r-fontawesome
  - r-languageserver
  - r-leaflet
  - r-lintr
  - r-precommit
  - pip:
      - editorconfig-checker
